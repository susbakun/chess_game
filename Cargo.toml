[package]
name = "chess_game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = {version = "0.18.1", features = ["bevy_dev_tools", "mesh_picking"]}
stockfish = "0.2.11"
wasm-bindgen = "0.2.121"


[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
